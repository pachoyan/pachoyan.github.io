---
import { SITE_TITLE } from '../consts';
import PagefindConfig from 'astro-pagefind/components/PagefindConfig.astro';
import HeaderLink from './HeaderLink.astro';
import SocialIcon from './SocialIcon.astro';
---

<header>
	<PagefindConfig />
	<nav>
		<h2>
			<a href="/">
				<img src="/avatar.svg" alt="" aria-hidden="true" class="title-avatar" />
				{SITE_TITLE}
			</a>
		</h2>
		<div class="internal-links">
			<HeaderLink href="/">Home</HeaderLink>
			<HeaderLink href="/blog">Blog</HeaderLink>
			<HeaderLink href="/tags">Tags</HeaderLink>
			<HeaderLink href="/about">About</HeaderLink>
		</div>
		<div class="social-links">
			<div class="header-search">
				<pagefind-searchbox></pagefind-searchbox>
			</div>
			<a href="https://pachoyan.com/cv/" target="_blank" rel="noopener noreferrer" class="cv-link">
				CV
			</a>
			<a
				href="https://www.linkedin.com/in/pachoyan/"
				target="_blank"
				rel="noopener noreferrer"
			>
				<span class="sr-only">LinkedIn</span>
				<SocialIcon name="linkedin" size={28} />
			</a>
			<a
				href="https://medium.com/@pachoyan"
				target="_blank"
				rel="noopener noreferrer"
			>
				<span class="sr-only">Medium</span>
				<SocialIcon name="medium" size={28} />
			</a>
			<a href="https://github.com/pachoyan" target="_blank" rel="noopener noreferrer">
				<span class="sr-only">Go to my GitHub</span>
				<SocialIcon name="github" size={32} />
			</a>
		</div>
	</nav>
</header>
<style>
	header {
		margin: 0;
		padding: 0 1em;
		background: white;
		box-shadow: 0 2px 8px rgba(var(--black), 5%);
	}
	h2 {
		margin: 0;
		font-size: 1em;
	}

	h2 a,
	h2 a.active {
		text-decoration: none;
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
	}
	.title-avatar {
		width: 28px;
		height: 28px;
		border-radius: 50%;
		object-fit: cover;
		border: 1px solid rgb(var(--gray-light));
	}
	nav {
		display: flex;
		align-items: center;
		justify-content: space-between;
	}
	nav a {
		padding: 1em 0.5em;
		color: var(--black);
		border-bottom: 4px solid transparent;
		text-decoration: none;
	}
	nav a.active {
		text-decoration: none;
		border-bottom-color: var(--accent);
	}
	.social-links,
	.social-links a {
		display: flex;
	}
	.social-links {
		align-items: center;
		gap: 0.5rem;
	}
	.header-search {
		min-width: 220px;
		max-width: 280px;
	}
	:global(.header-search .pagefind-ui) {
		font-size: 0.9rem;
	}
	:global(.header-search .pagefind-ui__search-input) {
		padding: 0.35rem 0.55rem;
	}
	.social-links a {
		align-items: center;
		justify-content: center;
	}
	.social-links .cv-link {
		padding: 0.35rem 0.7rem;
		border: 1px solid rgb(var(--gray-light));
		border-radius: 999px;
		font-size: 0.8rem;
		font-weight: 600;
		line-height: 1;
		border-bottom: 1px solid rgb(var(--gray-light));
	}
	.social-links .cv-link:hover {
		border-color: var(--accent);
	}
	@media (max-width: 720px) {
		nav {
			flex-wrap: wrap;
			gap: 0.6rem;
		}
		.internal-links {
			order: 3;
			width: 100%;
		}
		.social-links {
			order: 2;
			width: 100%;
			justify-content: space-between;
			display: flex;
		}
		.header-search {
			min-width: 0;
			max-width: 100%;
			flex: 1;
		}
	}
</style>
